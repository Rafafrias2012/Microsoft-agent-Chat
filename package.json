{
    "name": "msagent-chat-monorepo",
    "private": true,
    "workspaces": [
        "msagent.js",
        "protocol",
        "server", 
        "webapp"
    ],
    "scripts": {
        "start": "bun run start -w msagent.js -w server -w webapp",
        "build": "bun run build -w msagent.js -w protocol -w server -w webapp",
        "dev": "bun run dev -w msagent.js -w server -w webapp",
        "test": "bun test"
    },
    "type": "module",
    "engines": {
        "bun": ">=1.0.0"
    },
    "devDependencies": {
        "typescript": "^5.4.3"
    }
}
