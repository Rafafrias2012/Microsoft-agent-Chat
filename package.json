{
  "name": "msagent-chat",
  "version": "1.0.0",
  "scripts": {
    "start": "deno run --allow-net --allow-read --allow-write src/server/main.ts",
    "format": "deno fmt **/*.{ts,html}",
    "build": "deno compile src/server/main.ts",
    "test": "deno test"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "server",
    "webapp", 
    "protocol",
    "msagent.js"
  ]
}
