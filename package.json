{
    "name": "msagent-chat-monorepo",
    "private": true,
    "workspaces": [
        "msagent.js",
        "protocol",
        "server", 
        "webapp"
    ],
    "scripts": {
        "start": "npm run start -w msagent.js -w server -w webapp",
        "build": "npm run build -w msagent.js -w protocol -w server -w webapp",
        "dev": "npm run dev -w msagent.js -w server -w webapp",
        "test": "npm run test -w msagent.js -w protocol -w server -w webapp",
        "lint": "npm run lint -w msagent.js -w protocol -w server -w webapp"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=10.0.0"
    },
    "devDependencies": {
        "typescript": "^5.4.3",
        "eslint": "^8.57.0"
    }
}
